# sakichat 开发思路与原理文档

> 本文档记录 sakichat 从零实现的完整思路、原理知识点，以及每一步对应的代码文件。
> 配合 `server/`、`client/` 下的源码阅读。

---

## 一、实现步骤总览

| 步骤 | 做什么 | 对应文件 | 核心知识点 |
|------|--------|----------|------------|
| 1 | 实现 WebSocket 协议层 | `server/websocket.h` | HTTP 握手、帧格式、掩码、粘包/拆包 |
| 2 | 实现 epoll 服务端 | `server/main.cpp` | epoll 非阻塞 I/O、边缘触发、双映射表 |
| 3 | 编写前端页面 | `client/index.html` | WebSocket API、事件驱动渲染 |
| 4 | 编译配置 | `server/Makefile` | 链接 OpenSSL |
| 5 | 说明文档 | `README.md` / 本文档 | — |

> 第 0 步（想法层）：先理解"聊天 = 服务器转发"，见下文"阶段 0"。

---

## 二、阶段 0：聊天软件的本质

### 实现思路

聊天必须有人转发消息。如果 A 和 B 直接互连，就无法做到"谁在线、谁离线、转发给谁"。所以架构一定是：

```
浏览器A ──┐
          ├──→ 服务器（唯一知道所有人状态的地方）──→ 转发给 B
浏览器B ──┘
```

### 原理知识点

- **服务器是"唯一真相源"**：在线列表、用户映射、消息路由全在服务器内存里。
- **连接模型**：服务器 `listen` 一个端口（8080），每个浏览器 `connect` 过来就是一个 TCP 连接。服务器维护 N 个连接。
- 这是典型的 **C/S（客户端/服务器）架构**，和之前 `d:\tcp` 学的 echo 服务器同构，只是协议从"自造二进制"换成 WebSocket。

---

## 三、阶段 1：为什么必须用 WebSocket（而不是直接 TCP）

### 实现思路

已有的 `chat_server.cpp` 是裸 TCP + 自造协议（8 字节头 + JSON）。浏览器**根本不支持裸 TCP**——浏览器能用的只有 HTTP 和 WebSocket。

### 原理知识点

- **浏览器的安全模型**：网页只能发 HTTP/HTTPS（以及基于 HTTP 升级的 WebSocket），不能像 C 程序那样 `socket() + connect()` 到任意端口。
- **WebSocket 是"住在 HTTP 上的 TCP"**：它先走一次 HTTP 请求（握手），成功后把同一个 TCP 连接**升级**为全双工通信，之后双向随意推数据，不再像 HTTP 一样"一问一答"。
- 对比：HTTP 是"请求-响应"（每次要重连或长轮询）；WebSocket 是"连接建立后自由推流"——这正是聊天需要的（服务器要能主动把消息推给浏览器，而不等浏览器来问）。
- 结论：C++ 服务端**不必重写 epoll 框架**，只换"收发数据的格式"。

---

## 四、阶段 2：握手原理（HTTP 101 升级）

### 实现思路

浏览器 `new WebSocket("ws://localhost:8080")` 时，实际发出的不是二进制包，而是一段**普通 HTTP 文本请求**。服务器 `accept` 后读到的第一段数据就是它。

### 原理知识点

- 握手请求长这样（本质是 HTTP GET + 两个升级头）：

  ```
  GET /chat HTTP/1.1
  Upgrade: websocket
  Connection: Upgrade
  Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==
  ```

- 服务器回：

  ```
  HTTP/1.1 101 Switching Protocols
  Upgrade: websocket
  Connection: Upgrade
  Sec-WebSocket-Accept: <算出来的值>
  ```

- **为什么要 `Sec-WebSocket-Key` 这串校验**？不是加密，是**防缓存代理**：中间如有老式代理服务器，它可能不认识 Upgrade，把请求当普通 HTTP 缓存下来，导致后面帧数据被错乱转发。密钥校验让服务器确认"这条连接是真的升级请求"。
- **Accept 的计算**：`Accept = Base64(SHA1(key + "258EAFA5-E914-47DA-95CA-C5AB0DC85B11"))`。GUID 是 RFC 6455 写死的固定字符串。这解释了为什么服务端要链接 OpenSSL（算 SHA1）——协议层唯一的外部依赖。
- 握手成功后，**同一个 TCP 连接**开始传输帧数据，不再是文本行协议。

---

## 五、阶段 3：帧格式与掩码（协议层核心）

### 实现思路

握手之后的数据不是"裸文本"，而是被包成 **WebSocket 帧**。服务器要会拆帧、解掩码。

### 原理知识点

帧是二进制结构（逐位看）：

```
 0        1               2              3         4-7      8+
 +-+-+-+-+-------+-+-----+---------------+--------+---------+
 |F|R|R|R| opcode|M| len | extended len  | mask    | payload |
 |I|S|S|S| (4bit)|A|(7bit)| (0/2/8 bytes) | key(4B) | (数据)  |
 |N|V|V|V|       |S|      |               | 仅MASK=1 |        |
 +-+-+-+-+-------+-+-----+---------------+---------+---------+
```

- **FIN**：1 表示这是最后一片（消息可能分多帧发）。
- **opcode**：`0x1` 文本、`0x2` 二进制、`0x8` 关闭、`0x9` ping、`0xA` pong。
- **MASK**：**浏览器→服务器必须是 1**，服务器→浏览器必须 0。
- **len 的三档编码**：`<126` 直接读；`126` 表示后面 2 字节才是长度；`127` 表示后面 8 字节。这是为了"短消息省字节，长消息不溢出"。
- **掩码原理**：MASK=1 时，payload 与 4 字节 mask key **逐字节异或**还原。
- **为什么客户端必须掩码**（历史原因）：早期有攻击者利用"可预测的代理缓存"往 WebSocket 数据里注入 HTTP 响应头。掩码让中间代理无法预知/篡改有效载荷，属于**防投毒**措施，不是加密（服务端人人都能解开）。
- 实现上这就是 `websocket.h` 里 `try_parse` 做的事：**先看够不够 2 字节头 → 算长度 → 收够 → 解掩码 → 吐出文本**。

---

## 六、阶段 4：粘包/拆包（TCP 字节流特性）

### 实现思路

`read()` 一次读到的数据，可能只含半条消息，也可能含好几条。所以要有个缓冲区攒着，**凑够一条完整消息才解析**。

### 原理知识点

- **TCP 是字节流，没有消息边界**：`send("你好")` 和 `send("早上好")` 可能合并成一包到达，也可能拆成三截。
- 解决套路（协议里叫"**长度前缀**"）：本协议用帧头里的 len 字段知道"这条消息多长"。
- 拆包逻辑三问：
  1. 头够吗？不够 → 继续攒。
  2. 按 len 算，体够吗？不够 → 继续攒。
  3. 够了 → 取走这条，**继续循环**看缓冲区还有没有下一条（一包可能有多条）。
- 这就是 `try_parse` 要写成 `while (try_parse(...))` 的原因。

---

## 七、阶段 5：epoll 高并发模型

### 实现思路

服务器同时挂几十上百个连接，不能为每个连接开一个线程（资源贵、线程切换慢）。用 **epoll** 单线程管所有连接。

### 原理知识点

- **阻塞 read 的问题**：若一个线程 `read()` 某连接，数据没到就卡死，别的连接全被耽误。
- **非阻塞 + 事件通知**：把 fd 设为非阻塞，注册进 epoll；`epoll_wait()` 阻塞等"有数据可读"的通知，返回一批就绪 fd，逐个处理。空闲时 CPU 不空转。
- **边缘触发 EPOLLET vs 水平触发**：
  - 水平触发（LT）：数据没读完，下次 `epoll_wait` 还会再通知你 → 代码简单，但可能重复唤醒。
  - 边缘触发（ET）：只在"从无到有"那一刻通知一次 → 必须**一次循环读到返回 EAGAIN**，否则丢数据。性能好、代码要小心。
- 这解释了为什么 `read()` 返回 `-1` 且 `errno==EAGAIN` 就表示"读完了"（非阻塞下没数据可读）。
- 对比：typechat 的 `fork-per-client` 并发模型（每来一个连接 fork 一个进程）在连接多时进程数爆炸，epoll 是单进程撑万级连接的主流方案。

---

## 八、阶段 6：业务协议设计（登录/群聊/私聊）

### 实现思路

有了帧层，上面就传**纯 JSON 字符串**，用 `type` 字段区分消息类型，服务器按类型分发。

### 原理知识点

- **协议设计原则**：客户端→服务器用"命令"（login/chat/online_list），服务器→客户端用"事件"（login_ok/chat/system/online_list）。命令是"请做某事"，事件是"发生了某事"，两者分离。
- **两张映射表**：
  - `fd_to_user`：socket 文件描述符 → 用户名（这条连接是谁）
  - `user_to_fd`：用户名 → fd（**反向索引**，私聊时按名字查连接）
  - 为什么需要反向？收消息时只能拿到 fd，要查"是谁"；发私聊时只知道名字，要查"连到哪"。两条路都要 O(1)。
- **广播 vs 定向**：群聊遍历所有在线 fd 发；私聊先查 `user_to_fd["jerry"]`，只发给那一个连接，**同时发一份给发送者自己**（本地立即显示，否则自己的消息要等回显）。
- **在线列表刷新策略**：登录/退出时服务器主动给所有在线用户推 `online_list`——这是"服务器主动推"的典型场景，HTTP 做不到，WebSocket 天生擅长。
- **断连清理**：`read()==0` 表示对端关闭，此时要删两张表、广播离线、刷列表——否则会出现"幽灵在线用户"。

---

## 九、阶段 7：前端原理

### 实现思路

一个 `index.html` 搞定：`new WebSocket()` 连接 → 登录 → 渲染消息气泡 → 输入框发送。

### 原理知识点

- **WebSocket API 是事件驱动**：`onopen`（连上了）、`onmessage`（收到消息）、`onclose`（断了）、`send()`（发送）。不用轮询服务器。
- **UI = 状态 + 事件**：收到 `online_list` 就重渲染左侧列表；收到 `chat` 就 append 一条气泡。数据来一次，界面动一次。
- **消息气泡方向**：`from === myName` 决定靠左还是靠右（自己靠右、别人靠左）——视觉上就是 QQ/微信的样式。
- **私聊切换是前端状态**：点在线列表某个人 → 记 `target`，发送时 JSON 里带 `to: target`；再点一次取消回群聊。服务端只按 `to` 路由，不关心 UI。
- **XSS 注意**：用户输入拼进 HTML 前要转义（`<` `>` `&`），否则别人发 `<img onerror=...>` 能执行脚本——这是聊天软件必踩的安全坑。

---

## 十、阶段 8：进阶原理（心跳/离线/数据库）

- **心跳保活**：TCP 断线不一定能感知（拔网线、路由器超时），而且 NAT 会回收长期无流量的连接。客户端定时发 `{"type":"heartbeat"}`，服务器记录 `last_seen`，超时没心跳就判定死亡踢掉。WebSocket 层也可用 ping/pong 帧（opcode 0x9/0xA）做。
- **离线消息**：私聊对象不在线时，消息先存数据库，对方登录后查库推送。服务器要"内存状态 + 磁盘持久化"两层。
- **数据库（SQLite）**：用户表（密码**哈希**存储，不能存明文）、消息表（from/to/time/text）。哈希的意义：数据库泄露时攻击者也拿不到明文密码。
- **部署**：服务器放 Linux，Nginx 反代 WebSocket（`proxy_set_header Upgrade $http_upgrade`），域名 + HTTPS 后前端 `ws://` 变 `wss://`。

---

## 十一、整体脉络

**阶段 0-1** 定架构 → **阶段 2-4** 解决"浏览器怎么和 C++ 通信"（握手、帧、粘包）→ **阶段 5** 解决"怎么同时服务很多人"（epoll）→ **阶段 6-7** 解决"业务逻辑和界面"（JSON 协议 + 前端渲染）→ **阶段 8** 让它变"真"（保活/持久化/安全）。
